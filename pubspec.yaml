name: unified_sounds
description: A new Flutter project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dart_vlc: any
  just_audio: any
  path_provider: any
  uuid: any
  intl: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  import_sorter: any

import_sorter:
  comments: false
